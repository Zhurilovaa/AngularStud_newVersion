/*import { createSelector } from "@ngrx/store";
import { Student } from "src/app/tableStudmodul/student";
import { StudentState } from "../student.state";

//селектор получения массива сутдентов
const selectStud = (state: StudentState) => state.students;

export const selectStudList = createSelector(
  selectStud,
  (selectStud: Student[])=> selectStud
);*/